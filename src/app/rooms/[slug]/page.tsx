"use client";import {FormEvent,useEffect,useState} from "react";import {createClient} from "@/lib/supabase/client";type P={id:string,label:string,description:string};type B={human_contributions:string|null;assisted_synthesis:string|null;is_published:boolean};type R={id:string;slug:string;questions:{title:string;context:string|null;objective:string}|null;room_perspectives:P[];human_briefs:B|null};export default function Room({params}:{params:Promise<{slug:string}>}){const[room,setRoom]=useState<R|null>(null),[slug,setSlug]=useState(""),[msg,setMsg]=useState("");useEffect(()=>{params.then(x=>setSlug(x.slug))},[params]);useEffect(()=>{if(!slug)return;createClient().from("rooms").select("id,slug,questions(title,context,objective),room_perspectives(id,label,description),human_briefs(human_contributions,assisted_synthesis,is_published)").eq("slug",slug).single().then(({data,error})=>{if(error)setMsg(error.message);else setRoom(data as unknown as R)})},[slug]);async function apply(e:FormEvent<HTMLFormElement>){e.preventDefault();if(!room)return;const f=new FormData(e.currentTarget),s=createClient();const{data:{user}}=await s.auth.getUser();if(!user){window.location.href="/login";return}const{error}=await s.rpc("apply_to_room",{p_room_id:room.id,p_perspective_id:String(f.get("perspective")||"")||null,p_contribution:String(f.get("contribution")||"")});setMsg(error?error.message:"Application sent. The host will review your contribution.")}if(!room)return <main className="wrap"><p>{msg||"Loading room…"}</p></main>;const brief=room.human_briefs?.is_published?room.human_briefs:null;return <main className="wrap"><div className="hero"><div className="eyebrow">HUMAN ROOM · {room.questions?.objective?.toUpperCase()}</div><h1>{room.questions?.title}</h1><p>{room.questions?.context}</p></div>{brief&&<section className="card"><div className="eyebrow">HUMAN BRIEF</div><h2>What this room made clearer</h2>{brief.human_contributions&&<p>{brief.human_contributions}</p>}{brief.assisted_synthesis&&<p>{brief.assisted_synthesis}</p>}</section>}<div className="grid">{room.room_perspectives.map(p=><article className="card" key={p.id}><h3>{p.label}</h3><p>{p.description}</p></article>)}</div><form className="card" onSubmit={apply}><h2>What would you add to this conversation?</h2><label>Perspective<select name="perspective" required style={{display:"block",width:"100%",padding:14,marginTop:8}}><option value="">Choose one</option>{room.room_perspectives.map(p=><option value={p.id} key={p.id}>{p.label}</option>)}</select></label><textarea name="contribution" maxLength={300} minLength={20} required rows={5} placeholder="20–300 characters"/><button className="cta">APPLY FOR A SEAT</button>{msg&&<p role="status">{msg}</p>}</form></main>}
