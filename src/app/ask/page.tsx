const stages=["Explore","Question","Build","Act"];export default function Ask(){return <main className="wrap"><div className="hero"><div className="eyebrow">CREATE A ROOM</div><h1>Start with the question.</h1><p>The platform will identify which human perspectives this question needs.</p></div><form className="card"><label>What do you want humans to think about together?</label><textarea required minLength={10} maxLength={240} rows={4}/><label>What does this question need now?</label><div className="grid">{stages.map(s=><label className="card" key={s}><input type="radio" name="stage" required/> {s}</label>)}</div><button className="cta" type="submit">CONTINUE</button></form></main>}
