"use client";
import {FormEvent,useState} from "react";import {createClient} from "@/lib/supabase/client";
export default function Signup(){const[msg,setMsg]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);if(f.get("adult")!=="yes"){setMsg("Human Room is currently for adults 18+.");return}const email=String(f.get("email")||""),password=String(f.get("password")||"");const supabase=createClient();const{data,error}=await supabase.auth.signUp({email,password,options:{emailRedirectTo:window.location.origin+"/onboarding"}});if(error){setMsg(error.message);return}if(data.session){window.location.href="/onboarding";return}setMsg("Check your email to confirm your account, then continue to onboarding.")}return <main className="wrap"><div className="hero"><div className="eyebrow">JOIN HUMAN ROOM</div><h1>Bring a perspective.</h1></div><form className="card" onSubmit={submit}><label>Email<input name="email" type="email" required style={{display:"block",width:"100%",padding:14,margin:"8px 0 18px"}}/></label><label>Password<input name="password" type="password" required minLength={8} style={{display:"block",width:"100%",padding:14,margin:"8px 0 18px"}}/></label><label><input name="adult" value="yes" type="checkbox" required/> I confirm I am 18 or older.</label><br/><button className="cta">CREATE ACCOUNT</button>{msg&&<p role="status">{msg}</p>}</form></main>}