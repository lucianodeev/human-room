"use client";
import { FormEvent,useState } from "react";import { createClient } from "@/lib/supabase/client";
export default function Login(){const [msg,setMsg]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const email=String(f.get("email")||"");const password=String(f.get("password")||"");const supabase=createClient();const {data,error}=await supabase.auth.signInWithPassword({email,password});if(error){setMsg(error.message);return}const {data:profile}=await supabase.from("profiles").select("is_adult").eq("id",data.user.id).maybeSingle();window.location.href=profile?.is_adult?"/rooms":"/onboarding"}return <main className="wrap"><div className="hero"><div className="eyebrow">HUMAN ROOM</div><h1>Sign in.</h1><p>Your identity protects the quality and safety of each room.</p></div><form className="card" onSubmit={submit}><label>Email<input name="email" type="email" required style={{display:"block",width:"100%",padding:14,margin:"8px 0 18px"}}/></label><label>Password<input name="password" type="password" required minLength={8} style={{display:"block",width:"100%",padding:14,margin:"8px 0 18px"}}/></label><button className="cta">SIGN IN</button>{msg&&<p role="alert">{msg}</p>}</form></main>}