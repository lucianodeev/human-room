"use client";
import {FormEvent,useEffect,useState} from "react";import {useRouter} from "next/navigation";import {createClient} from "@/lib/supabase/client";
export default function Onboarding(){const router=useRouter(),[msg,setMsg]=useState(""),[ready,setReady]=useState(false);
useEffect(()=>{(async()=>{const s=createClient();const{data:{user}}=await s.auth.getUser();if(!user){router.replace("/login");return}const{data}=await s.from("profiles").select("is_adult").eq("id",user.id).maybeSingle();if(data?.is_adult){router.replace("/rooms");return}setReady(true)})()},[router]);
async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);if(f.get("adult")!=="yes"){setMsg("Human Room is currently for adults 18+.");return}const s=createClient(),{data:{user}}=await s.auth.getUser();if(!user){router.replace("/login");return}const profile={id:user.id,display_name:String(f.get("display_name")||"").trim()||"Participante",city:String(f.get("city")||"").trim()||"Brussels",languages:[String(f.get("language")||"en")],is_adult:true};const{error}=await s.from("profiles").upsert(profile,{onConflict:"id"});if(error){setMsg(error.message);return}router.replace("/rooms");router.refresh()}
if(!ready)return <main className="wrap"><p>Preparing your profile…</p></main>;
return <main className="wrap"><div className="hero"><div className="eyebrow">WELCOME</div><h1>Your perspective starts here.</h1><p>Complete the minimum profile needed to participate. No popularity score.</p></div><form className="card" onSubmit={submit}><label>Name or public name<input name="display_name" required minLength={2} style={{display:"block",width:"100%",padding:14,margin:"8px 0 18px"}}/></label><label>City<input name="city" defaultValue="Brussels" style={{display:"block",width:"100%",padding:14,margin:"8px 0 18px"}}/></label><label>Language<select name="language" defaultValue="en" style={{display:"block",width:"100%",padding:14,margin:"8px 0 18px"}}><option value="en">English</option><option value="fr">Français</option><option value="pt">Português</option><option value="es">Español</option></select></label><label><input name="adult" value="yes" type="checkbox" required/> I confirm I am 18 or older.</label><br/><button className="cta">CONTINUE</button>{msg&&<p role="status">{msg}</p>}</form></main>}